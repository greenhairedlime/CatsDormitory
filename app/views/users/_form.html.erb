<%= form_for @user do |f| %>

    <% if @user.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@user.errors.count, "error") %> prohibited
            this user from being saved:
          </h2>
          <ul>
            <% @user.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <p>
      <%= f.label :owner_last_name %><br>
      <%= f.text_field :owner_last_name %>
    </p>

    <p>
      <%= f.label :owner_first_name %><br>
      <%= f.text_field :owner_first_name %>
    </p>

    <p>
      <%= f.label :pet_name %><br>
      <%= f.text_field :pet_name %>
    </p>

    <p>
      <%= f.label :text %><br>
      <%= f.text_area :text %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>